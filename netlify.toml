# Netlify configuration for the Presstropic landing page.
[build]
  publish = "."

# After a successful inquiry submission, send the visitor back to the page
# with a success message shown inline.
[[redirects]]
  from = "/inquiry-success"
  to = "/index.html?submitted=true"
  status = 302
